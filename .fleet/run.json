{
    "configurations": [
        {
            "type": "command",
            "name": "Run GitHub Workflow (Works only on Linux)",
            "program": "/bin/bash",
            "workingDir": "./",
            "args": [
                "-c",
                "FILE=./.fleet/act.sh && chmod +x $FILE && $FILE"
            ]
        }
    ]
}